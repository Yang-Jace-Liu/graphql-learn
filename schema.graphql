type User {
    id: Int!
    full_name: String!
    email: String!
    gender: String!
    date_of_birth: String!
    country: Country!
    created_at: String
}

type Country {
    code: Int!
    name: String!
    continent_name: String!
}

type Query {
    user(id: Int!): User
    users: [User]
}
